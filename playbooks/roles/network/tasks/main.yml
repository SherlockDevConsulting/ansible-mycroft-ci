- name: Configure Static IP
  template:
    src: netplan.j2
    dest: /etc/netplan/50-cloud-init.yaml
  notify: Apply Netplan

# - name: Ensure netplan is applied # -> in handler
#   command: sudo netplan apply
