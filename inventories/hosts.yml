all:
  hosts:
    mycroft-ci:
      ansible_host: 192.168.1.100 # IP address of the server
      ansible_user: sherlock
      ansible_ssh_private_key_file: ~/.ssh/id_rsa
  children:
    devops:
      hosts:
        mycroft-ci

